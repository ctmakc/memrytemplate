# Archive

Cold storage for information that should remain retrievable but should not occupy normal working context.

Recommended structure:

```text
archive/
  cases/<domain>/<project-or-client>/...
  checkpoints/<case-id>/<YYYY>.md
```

Do not routinely load this directory. Search it only when historical detail is needed.