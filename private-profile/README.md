# Private raw profile sources

**Do not place personal information here while the repository is public.**

After forking and making the repository private, this folder may contain source material such as:

- `interview-full.md` — full uncompressed onboarding interview;
- imported professional background notes;
- other raw material explicitly intended for memory bootstrapping.

Raw sources are not default context. Distill durable information into `profile/` and project-specific information into `cases/`. Read raw sources later only when exact historical wording/detail is required.